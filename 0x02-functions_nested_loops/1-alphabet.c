#include<stdio.h>
/*
 *print_alphabet - prints alphabet in lowercase
 *return -always successful
 */
void print_alphabet()
{
        char i;

        for (i = 'a';i<='z';i++)
        {
                putchar(i);

        }

        putchar(\n);
}
